# kanji-test-generator

小学生向け漢字書き取りテスト作成アプリ。GitHub Pagesでの公開を想定した静的サイト。

## 構成

- ビルド不要の素のHTML/CSS/JS(`index.html` / `style.css` / `script.js` / `data.js`)
- `data.js` に学年(1〜6)ごとの漢字データを配列で保持。各エントリは `{ kanji, grade, words: [{reading, answer}] }`
- テスト形式は「書き取り」のみ(読みを見て紙に鉛筆で漢字を書く想定)。入力フォームは実装しない
- 漢字選択は学年単位ではなく1文字単位。学年フィルタ・検索はあくまで絞り込みUIで、選択状態(`Set`)は学年をまたいで保持される
- 印刷用CSS(`@media print`)で操作パネル(`.no-print`)を隠し、解答一覧は `page-break-before` で別ページにしている
- 各問題に罫線付きの書き込みボックス(answer-box)は無い。紙に自由に書く前提なので、読み仮名と余白だけを印刷する(ユーザーの明示的な要望。「解答欄」という言葉が指すものが曖昧だったので、削除対象を確認してから対応した)

## データ収録方針

- 全1006字ではなく、各学年20字ずつ・計120字の代表的セット(ユーザーの希望により「代表的な問題セットをこちらで作成」を選択)
- 各漢字に単語2つ(訓読み単語+音読み熟語など)を用意。単語は基本的にその漢字のみ(+送り仮名)で完結する語を選び、他の未習漢字への依存を避けている
- 追加する場合は同じスキーマで `data.js` に追記するだけで自動反映される

## 漢字選択UIの一括操作(50字選ぶような場面向け)

1個ずつチェックするのは大変という指摘を受けて追加した機能。1文字単位選択の方針は変えず、選ぶ手間だけ減らす方向で実装。

- 学年見出し(`.grade-group-title`)自体がチェックボックス付きラベルで、その学年(表示中の絞り込み後の集合)をワンクリックで全選択/全解除できる。一部だけ選択済みなら `indeterminate` で「－」表示にして状態を可視化
- グリッド内で `shift+click` すると、直前にクリックした項目から今回の項目までを範囲選択できる(ファイラー的なUX)。アンカー(`lastClickedIndex`)は学年フィルタ・検索文字列を変えたときだけリセットする。同じ絞り込み状態でチェックの有無が変わっても`flatEntries`の並びは不変なので、都度リセットする必要はない
- 貼り付け欄(`#pasteBox`)にまとめて漢字を打ち込み/貼り付けて「追加」「除外」ボタンで一括反映できる。データに無い文字は無視してalertで通知する
- 選択中の漢字はチップ一覧(`#selectedChips`)として学年フィルタに関係なく常時表示され、クリックで個別解除できる。50字選んだ後の確認・微調整はここで行う想定
- 個別チェックボックスのトグルは`renderPicker()`を呼ばずDOMを直接書き換えている(`.kanji-picker`はスクロール領域なので、毎回全体再描画するとスクロール位置が先頭に戻ってしまうため)。学年一括選択・シフト範囲選択・貼り付け一括操作など「複数まとめて変える」操作のときだけ`renderPicker()`で全体再描画している
- `shift+click`はキーボード修飾キー依存なのでタブレットのタップでは使えない(ユーザーからの質問で発覚)。代わりに「範囲選択モード」トグルボタン(`#rangeModeBtn`)を追加し、オンの状態で1回目のタップを起点(`rangeAnchorIndex`、`.range-anchor`クラスで緑枠表示)、2回目のタップで範囲確定という「タップタップ」方式にした。PCのshift+clickと共存させており、`rangeMode`がtrueのときはitemのclickハンドラ内で最優先分岐してshift判定より前に処理する

## 動作確認の方法

- Playwrightがグローバルに入っていない環境。`npm install playwright --no-save --prefix /tmp/pw_env` のようにローカルインストールし `NODE_PATH=/tmp/pw_env/node_modules node script.js` で実行するとブラウザ動作を検証できる
- `chromium-cli` はこの環境には無い

## デプロイ

- git init・GitHubリポジトリ作成・pushは未実施(2026-07-25時点、ユーザーの意向で保留)。手順は `README.md` に記載済み
- 進める際はSettings → Pages → Deploy from branch (main / root) でOK、ビルドステップ不要
